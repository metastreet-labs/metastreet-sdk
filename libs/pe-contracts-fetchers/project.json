{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/pe-contracts-fetchers/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/pe-contracts-fetchers",
        "main": "libs/pe-contracts-fetchers/src/index.ts",
        "tsConfig": "libs/pe-contracts-fetchers/tsconfig.lib.json",
        "assets": ["libs/pe-contracts-fetchers/*.md"]
      }
    },
    "publish": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs pe-contracts-fetchers {args.ver} {args.tag}"
      },
      "dependsOn": [
        {
          "projects": "self",
          "target": "build"
        }
      ]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/pe-contracts-fetchers/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/pe-contracts-fetchers"],
      "options": {
        "jestConfig": "libs/pe-contracts-fetchers/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
